Splash assets are intentionally stored as normalized 256x256 transparent PNGs:
- munib_splash_dark.png: bright turquoise logo for dark mode.
- munib_splash_light.png: dark green logo for light mode.
Both are normalized to the same visual width so they render at the same apparent size.
